<article class="card">
  <h2><%= book.title %></h2>
  <p class="book-info">
    By
    <i
      ><%= book.author_name%> - <%= book.release_date.toLocaleDateString() %> -
      <%= book.pages %> pages
    </i>
  </p>
  <p><%= book.summary %></p>

  <%- include("./actions", {route: "books", id: book.id}) %>
</article>
